const holes=[...document.querySelectorAll('.hole')],scoreBoard=document.querySelector('.score'),moles=[...document.querySelectorAll('.mole')],button=document.querySelector('#start'),timeEl=document.querySelector('#time'),timeBar=document.querySelector('#time-bar'),comboEl=document.querySelector('#combo'),DURATION=30000;let lastHole=null,timeUp=true,score=0,combo=0,run=0,tickId=null;const randomTime=(min,max)=>Math.round(Math.random()*(max-min)+min);function randomHole(){let hole;do{hole=holes[Math.floor(Math.random()*holes.length)]}while(hole===lastHole&&holes.length>1);lastHole=hole;return hole}function updateHud(){scoreBoard.textContent=String(score);comboEl.textContent='×'+combo;comboEl.classList.toggle('hot',combo>=5);window.parent.postMessage({__hub:1,type:'score',value:score},'*')}function pace(){const level=Math.min(score,35);return{min:Math.max(170,410-level*5),max:Math.max(390,880-level*11)}}function mood(){return combo>=10?'hot':combo>=5?'combo':'normal'}function peep(id){if(timeUp||id!==run)return;const p=pace(),time=randomTime(p.min,p.max),hole=randomHole();hole.classList.remove('missed');hole.dataset.mood=mood();hole.classList.add('up');setTimeout(()=>{if(id!==run)return;const missed=hole.classList.contains('up');hole.classList.remove('up');if(missed){combo=0;updateHud();hole.classList.add('missed');const ring=document.createElement('span');ring.className='miss-ring';hole.appendChild(ring);setTimeout(()=>ring.remove(),380);setTimeout(()=>hole.classList.remove('missed'),210)}if(!timeUp)peep(id)},time)}function finishRun(id){if(id!==run||timeUp)return;timeUp=true;clearInterval(tickId);tickId=null;holes.forEach((h)=>{h.classList.remove('up');delete h.dataset.mood});timeEl.textContent='0';timeBar.style.width='0%';button.textContent='Ещё раунд';button.hidden=false;window.parent.postMessage({__hub:1,type:'finish',score},'*')}function startGame(){run++;const id=run;timeUp=false;score=0;combo=0;lastHole=null;updateHud();button.hidden=true;timeEl.textContent='30';timeBar.style.width='100%';const end=performance.now()+DURATION;clearInterval(tickId);tickId=setInterval(()=>{const left=Math.max(0,end-performance.now());timeEl.textContent=String(Math.ceil(left/1000));timeBar.style.width=left/DURATION*100+'%';if(left<=0)finishRun(id)},100);peep(id)}function bonk(e){if(!e.isTrusted||timeUp)return;const hole=this.parentElement;if(!hole.classList.contains('up'))return;score++;combo++;hole.classList.remove('up');hole.classList.remove('hit');void hole.offsetWidth;hole.classList.add('hit');const burst=document.createElement('span');burst.className='burst';burst.textContent=combo>=5?`+1  ×${combo}`:'+1';hole.appendChild(burst);const fx=document.createElement('span');fx.className='hit-fx';fx.innerHTML='<i></i><i></i><i></i><i></i>';hole.appendChild(fx);setTimeout(()=>{burst.remove();fx.remove();hole.classList.remove('hit')},450);updateHud()}button.addEventListener('click',startGame);moles.forEach((mole)=>{mole.addEventListener('click',bonk);mole.addEventListener('touchstart',(e)=>{e.preventDefault();bonk.call(mole,e)},{passive:false})});
