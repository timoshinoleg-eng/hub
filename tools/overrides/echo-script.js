const COLORS=['green','red','yellow','blue'];const buttons=COLORS.map((id)=>document.getElementById(id));const startBtn=document.getElementById('start-btn');const phaseEl=document.getElementById('phase');const levelEl=document.getElementById('level');const completedEl=document.getElementById('completed');const messageEl=document.getElementById('level-message');let sequence=[],level=0,completed=0,userIndex=0,accepting=false,run=0,__hubDone=false;const sleep=(ms)=>new Promise((resolve)=>setTimeout(resolve,ms));const hubScore=(s)=>window.parent.postMessage({__hub:1,type:'score',value:s},'*');function hubFinish(s){if(__hubDone)return;__hubDone=true;window.parent.postMessage({__hub:1,type:'finish',score:s},'*')}function setPhase(text){phaseEl.textContent=text}function flash(color){const b=document.getElementById(color);b.classList.add('active');setTimeout(()=>b.classList.remove('active'),230)}function updateHud(){levelEl.textContent=String(level);completedEl.textContent=String(completed)}async function playSequence(id){accepting=false;setPhase('Смотри внимательно…');messageEl.textContent='';messageEl.className='';const gap=Math.max(300,560-level*18);await sleep(220);for(const color of sequence){if(id!==run)return;flash(color);await sleep(gap)}if(id!==run)return;userIndex=0;accepting=true;setPhase('Твоя очередь')}function nextLevel(id){if(id!==run)return;level++;sequence.push(COLORS[Math.floor(Math.random()*COLORS.length)]);updateHud();playSequence(id)}function startGame(){run++;sequence=[];level=0;completed=0;userIndex=0;accepting=false;__hubDone=false;updateHud();hubScore(0);startBtn.hidden=true;startBtn.textContent='Ещё раз';nextLevel(run)}function fail(color){accepting=false;completed=Math.max(0,level-1);updateHud();setPhase('Ошибка — но ритм уже в памяти');messageEl.textContent=`Нужен был цвет: ${color==='green'?'зелёный':color==='red'?'красный':color==='yellow'?'жёлтый':'синий'}`;messageEl.className='lose';flash(color);hubFinish(completed);startBtn.hidden=false;startBtn.textContent='Ещё раз'}function press(color){if(!accepting)return;flash(color);const expected=sequence[userIndex];if(color!==expected){fail(expected);return}userIndex++;if(userIndex===sequence.length){accepting=false;completed=level;updateHud();hubScore(completed);setPhase('Точно! Следующий ритм…');messageEl.textContent=`Уровень ${level} пройден ✦`;messageEl.className='';const id=run;setTimeout(()=>nextLevel(id),620)}}startBtn.addEventListener('click',startGame);buttons.forEach((b)=>b.addEventListener('pointerdown',(e)=>{e.preventDefault();press(b.id)}));updateHud();
