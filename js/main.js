const revision=window.HUB_ASSET_REVISION||'20260911-rc1';const { bridge }=await import(`./bridge.js?v=${encodeURIComponent(revision)}`);
import { track, hasConsent, setConsent, subscribe, subscriptionAvailable } from './track.js';
import { cardDataUrl, shareText } from './share.js';
import { duelResult } from './duel.js';
import { dailySeed } from './daily.js';
import { observeVisit } from './engagement.js';
import { getGameProgress, getSummary, recordFinish } from './progress.js';
import { challengeIntroText,challengeResultState,dailyHeroState,dailyResultText,recordBadgeText } from './ui-state.js';
import { GAMES, byId, visible } from './games.js';
const CFG=window.HUB_CONFIG||{},SHOW_ALL=new URLSearchParams(location.search).has('all'),HUB_NAME=CFG.hubName||'Игротека',$=(s)=>document.querySelector(s);const el=(tag,cls,html)=>{const n=document.createElement(tag);if(cls)n.className=cls;if(html!=null)n.innerHTML=html;return n};const state={game:null,score:null,challenge:null,finishMeta:null};window.__hubStartParam=bridge.startParam();
function esc(s){return String(s??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function iconSvg(id,cls='game-icon'){const safe=/^[a-z-]+$/.test(String(id||''))?id:'brand';return `<svg class="${cls}" aria-hidden="true"><use href="assets/icons.svg#${safe}"></use></svg>`}
function mascotSvg(stateName='idle',cls='mascot-icon'){const safe=['idle','happy','wow','challenge','fail'].includes(stateName)?stateName:'idle';return iconSvg(`mascot-${safe}`,cls)}function sceneSvg(id,cls='scene-icon'){const safe=/^[a-z]+$/.test(String(id||''))?id:'merge';return iconSvg(`scene-${safe}`,cls)}
function formatBest(g,best){return best==null?'Рекорда ещё нет':`Рекорд ${best}${g.unit?' '+g.unit:''}`}function deepLink(gameId,score){const bot=CFG.bot;if(!bot)return'';const p=score==null?`g${gameId}`:`g${gameId}_s${score}`;return `https://max.ru/${bot}?startapp=${p}`}
function parseStartParam(sp){const m=/^g([a-z]+)(?:_s(\d+))?$/i.exec(sp||'');if(!m)return null;const game=byId(m[1].toLowerCase());return game?{game,challenge:m[2]?Number(m[2]):null}:null}
function dailyGameForDate(date){const pool=GAMES.filter((g)=>g.enabled&&g.cfg?.daily);if(!pool.length)return null;const n=[...date].reduce((a,c)=>a+c.charCodeAt(0),0);return pool[n%pool.length]}
function renderMenu(){const today=dailySeed(),summary=getSummary(today),daily=dailyGameForDate(today),all=visible(SHOW_ALL),catalog=all.filter((g)=>g.id!==daily?.id);$('#streak-chip').innerHTML=summary.streak>0?`<strong>${summary.streak}</strong><span>дн. подряд</span>`:'<span class="streak-empty">Начни<br>серию</span>';if(daily){const pg=getGameProgress(daily.id),heroState=dailyHeroState(pg,today,summary.completedToday),mascotState=heroState.completed?'happy':'challenge';$('#daily-card').innerHTML=`<div class="daily-card${heroState.completed?' is-complete':''}" style="--d1:${daily.accent};--d2:${daily.accent2}"><div class="daily-mascot" aria-hidden="true">${mascotSvg(mascotState,'daily-mascot-icon')}</div><div class="daily-top"><span class="daily-label">✦ ВЫЗОВ ДНЯ</span><span class="daily-status">${esc(heroState.status)}</span></div><div class="daily-main"><div class="daily-emoji">${iconSvg(daily.icon,'daily-icon')}</div><div class="daily-copy"><h2>${esc(daily.title)}</h2><p>${esc(daily.tagline)} · ${esc(formatBest(daily,pg.best))}</p></div></div><button class="daily-play" id="daily-play">${esc(heroState.cta)}</button></div>`;$('#daily-play').onclick=()=>openGame(daily.id)}const progress=$('#progress-strip');if(summary.finishes===0){progress.classList.add('is-empty');progress.innerHTML=`<div class="progress-first"><span class="progress-first-mark">${mascotSvg('idle','progress-mascot')}</span><span><strong>Сыграй первую партию</strong><small>Рекорд и серия появятся здесь автоматически.</small></span></div>`}else{progress.classList.remove('is-empty');progress.innerHTML=`<div class="pstat"><strong>${summary.finishes}</strong><span>завершено игр</span></div><div class="pstat"><strong>${summary.records}</strong><span>личных рекордов</span></div><div class="pstat"><strong>${summary.streak}</strong><span>дней подряд</span></div>`}const list=$('#games');list.innerHTML='';for(const g of catalog){const pg=getGameProgress(g.id),card=el('button','gcard'+(g.enabled?'':' draft'));card.dataset.id=g.id;card.style.setProperty('--game',g.accent||'#7568ff');card.style.setProperty('--game2',g.accent2||g.accent||'#4ce3e8');card.innerHTML=`<span class="gscene" aria-hidden="true">${sceneSvg(g.id)}</span><div class="gcard-top"><span class="ge">${iconSvg(g.icon)}</span><span class="gbadge ${g.cfg?.daily?'daily':''}">${g.cfg?.daily?'✦ ЕЖЕДНЕВНО':esc(g.genre||g.length)}</span></div><span class="gt"><b>${esc(g.title)}</b><i>${esc(g.tagline)}</i></span><span class="gfoot"><span class="gbest">${esc(pg.best==null?g.length:formatBest(g,pg.best))}</span><span class="garrow">›</span></span>`;card.addEventListener('click',()=>openGame(g.id));list.appendChild(card)}$('#count').textContent=daily?`${catalog.length} игр + вызов`:`${catalog.length} игр`}
let offBack=()=>{},lastScoreHaptic=0;function tipSeen(id){try{return localStorage.getItem('hub_tip_'+id)==='1'}catch{return true}}function markTip(id){try{localStorage.setItem('hub_tip_'+id,'1')}catch{}}function hintVisual(g){const kind=/^[a-z]+$/.test(g?.hint||'')?g.hint:'tap';return `<div class="hint-visual hint-${kind}" aria-hidden="true">${sceneSvg(g.id,'hint-scene')}<span class="hint-gesture"></span></div>`}function showGameTip(g){if(!g?.howTo||tipSeen(g.id))return;markTip(g.id);const n=el('div','game-tip visual-tip');n.innerHTML=`${hintVisual(g)}<div class="hint-copy">${esc(g.howTo)}<small>${esc(g.length)} · результат сохранится автоматически</small></div>`;$('#overlay').appendChild(n);setTimeout(()=>n.remove(),3900)}function pulseScore(){const score=$('#game-score');if(!score||!score.textContent)return;score.classList.remove('score-pop');void score.offsetWidth;score.classList.add('score-pop')}
function openGame(id,challenge=null){const g=byId(id);if(!g)return;state.game=g;state.score=null;state.challenge=challenge;state.finishMeta=null;lastScoreHaptic=0;const pg=getGameProgress(g.id);document.documentElement.style.setProperty('--active-game',g.accent||'#6f63ff');$('#game-title').textContent=g.title;$('#game-meta').textContent=`${g.genre} · ${formatBest(g,pg.best)}`;$('#game-score').textContent='';let src=`games/${g.id}/index.html`;if(g.cfg?.daily)src+='?seed='+dailySeed();if(challenge!=null)src+=(src.includes('?')?'&':'?')+'challenge='+challenge;const frame=$('#game-frame');frame.style.opacity='0';frame.src=src;$('#overlay').innerHTML='';document.body.dataset.view='game';track('open_game',g.id);bridge.haptic('selection');offBack();offBack=bridge.onBack(backToMenu);setTimeout(()=>state.game?.id===g.id&&showGameTip(g),500)}
function backToMenu(){offBack();offBack=()=>{};$('#game-frame').src='about:blank';$('#game-score').textContent='';document.body.dataset.view='menu';state.game=null;state.challenge=null;renderMenu();track('back_to_menu')}
function onMessage(e){const d=e.data;if(!d||d.__hub!==1)return;const f=document.getElementById('game-frame');if(!f||e.source!==f.contentWindow)return;if(d.type==='ready'){if(!state.game||d.game!==state.game.id)return;f.contentWindow.postMessage({__hub:1,type:'cfg',game:d.game,cfg:state.game.cfg||{}},'*');requestAnimationFrame(()=>requestAnimationFrame(()=>{f.style.opacity='1'}));return}if(!state.game)return;if(d.type==='score'){const hadScore=state.score!=null,changed=String(state.score)!==String(d.value);state.score=d.value;$('#game-score').textContent=d.value;if(hadScore&&changed)pulseScore();const now=performance.now();if(hadScore&&changed&&now-lastScoreHaptic>140){bridge.haptic('selection');lastScoreHaptic=now}return}if(d.type==='finish'){state.score=d.score??state.score;const today=dailySeed();state.finishMeta=recordFinish(state.game,state.score??0,today);track('finish',state.game.id,state.score);if(state.finishMeta.newBest)track('new_record',state.game.id,state.score);if(state.finishMeta.dailyAdvanced)track('daily_complete',state.game.id,state.finishMeta.streak);showResult()}}
const NOTIFY_PROMPT_KEY='hub_notify_prompted_v1';function shouldOfferNotify(today){if(!subscriptionAvailable()||hasConsent()||getSummary(today).finishes<3)return false;try{return localStorage.getItem(NOTIFY_PROMPT_KEY)!=='1'}catch{return false}}function markNotifyPrompted(){try{localStorage.setItem(NOTIFY_PROMPT_KEY,'1')}catch{}}
function confettiHtml(){const colors=['#ff5f7e','#ffc760','#54e6d2','#7568ff','#fff'];return `<div class="confetti">${Array.from({length:22},(_,i)=>`<i style="--x:${(i*37)%100}%;--c:${colors[i%colors.length]};--d:${1.7+(i%5)*.18}s;--delay:${(i%7)*.04}s;--r:${(i*29)%180}deg;--drift:${-34+(i%9)*8}px"></i>`).join('')}</div>`}
function showResult(){const g=state.game;if(!g)return;const score=state.score??0,meta=state.finishMeta||{},link=deepLink(g.id,score),higherIsBetter=g.cfg?.higherIsBetter!==false,duel=duelResult(score,state.challenge,higherIsBetter),duelState=challengeResultState(duel,higherIsBetter),box=el('div','result finish-sweep');const duelLine=duelState?`<div class="rduel ${duelState.kind}">${esc(duelState.text)}</div>`:'';if(duel)track(duel.won?'duel_win':'duel_lose',g.id,score);const dailyText=dailyResultText(meta),dailyLine=dailyText?`<div class="rdaily">${esc(dailyText)}</div>`:'',recordBadge=recordBadgeText(meta),mascotState=duelState?.kind==='lose'?'fail':(meta.newBest||meta.dailyAdvanced||duelState?.kind==='win')?'happy':duelState?'challenge':'idle',shareBadge=meta.newBest?'НОВЫЙ РЕКОРД':meta.dailyAdvanced?'ВЫЗОВ ДНЯ':duelState?.kind==='win'?'ПОБЕДА':'';const offerNotify=shouldOfferNotify(dailySeed());if(offerNotify)markNotifyPrompted();box.innerHTML=`<div class="rcard" style="--result-glow:${g.accent||'#6d5cff'}"><div class="result-mascot" aria-hidden="true">${mascotSvg(mascotState,'result-mascot-icon')}</div>${meta.newBest?confettiHtml():''}${recordBadge?`<div class="record-pill">${esc(recordBadge)}</div>`:''}<div class="result-icon-wrap">${iconSvg(g.icon,'result-icon')}</div><div class="rttl">${esc(g.title)}</div><div class="result-score">${score}${g.unit?` <span style="font-size:14px;letter-spacing:0">${esc(g.unit)}</span>`:''}</div><div class="rbest">${meta.newBest?'Лучший результат сохранён':esc(formatBest(g,meta.best))}</div>${duelLine}${dailyLine}<div class="rrow"><button class="btn primary" id="r-again">Ещё раз</button><button class="btn" id="r-share">Поделиться</button></div><div class="result-links"><button class="result-link" id="r-menu">К играм</button>${offerNotify?'<button class="result-link" id="r-notify">Получать новинки</button>':''}</div><img class="rimg" alt="карточка результата"><div class="rhint" id="r-hint"></div></div>`;const resultCard=box.querySelector('.rcard'),resultImage=box.querySelector('.rimg'),resultHint=box.querySelector('#r-hint'),shareBtn=box.querySelector('#r-share');resultImage.src=cardDataUrl({gameId:g.id,title:g.title,score,unit:g.unit,hubName:HUB_NAME,accent:g.accent,accent2:g.accent2,badge:shareBadge});box.querySelector('#r-again').onclick=()=>{track('replay',g.id,score);box.remove();openGame(g.id,state.challenge)};box.querySelector('#r-menu').onclick=()=>{box.remove();backToMenu()};shareBtn.onclick=async()=>{const ok=await bridge.share(shareText({title:g.title,score,unit:g.unit,link}),link);track(ok?'share_ok':'share_fallback',g.id,score);if(ok){resultCard.classList.remove('share-fallback');resultHint.textContent='';resultImage.style.display='none';shareBtn.textContent='Поделиться';return}resultCard.classList.add('share-fallback');resultHint.textContent='Не удалось открыть шаринг. Карточка результата доступна ниже.';resultImage.style.display='block';shareBtn.textContent='Повторить шаринг'};const notifyBtn=box.querySelector('#r-notify');if(notifyBtn)notifyBtn.onclick=async()=>{if(!hasConsent()){showConsent(()=>doSubscribe(notifyBtn,g,score));return}await doSubscribe(notifyBtn,g,score)};$('#overlay').innerHTML='';$('#overlay').appendChild(box);bridge.haptic(meta.newBest?'notify':'selection')}
async function doSubscribe(btn,g,score){const r=await subscribe(g.id);track(r.ok?'notify_subscribe':'notify_failed',g.id,score);btn.textContent=r.ok?'✓ Подписка включена':'Не получилось';btn.disabled=r.ok;if(r.ok)bridge.haptic('notify')}
function showConsent(onAccept){const box=el('div','result consent-layer'),policy=CFG.policyUrl?` <a href="${CFG.policyUrl}" target="_blank" rel="noopener">Подробнее о данных</a>`:'';box.innerHTML=`<div class="rcard consent-card"><div class="consent-icon-wrap">${mascotSvg('idle','consent-icon')}</div><div class="consent-title">Включить игровые уведомления?</div><p>MAX передаст серверу подписанные данные приложения. Мы сохраним идентификатор только для отправки игровых новинок.${policy}</p><div class="rrow"><button class="btn primary" id="c-yes">Включить</button><button class="btn" id="c-no">Не сейчас</button></div></div>`;box.querySelector('#c-yes').onclick=()=>{setConsent();track('consent_yes');box.remove();onAccept?.()};box.querySelector('#c-no').onclick=()=>{track('consent_no');box.remove()};$('#overlay').appendChild(box)}
function init(){bridge.ready();bridge.expand();renderMenu();window.addEventListener('message',onMessage);$('#back').addEventListener('click',backToMenu);$('#reload').addEventListener('click',()=>state.game&&openGame(state.game.id,state.challenge));const visit=observeVisit();track('open_bot');if(visit.firstVisit)track('first_visit');else if(visit.returning)track('return_visit',null,visit.daysAway);const parts=['Мини-игры в MAX · без рекламы и покупок'];if(CFG.orgName)parts.push(CFG.orgName);const links=[];if(CFG.policyUrl)links.push(`<a href="${CFG.policyUrl}" target="_blank" rel="noopener">Политика</a>`);if(CFG.offerUrl)links.push(`<a href="${CFG.offerUrl}" target="_blank" rel="noopener">Оферта</a>`);$('#legal').innerHTML=parts.join(' · ')+(links.length?'<br>'+links.join(' · '):'');const sp=parseStartParam(window.__hubStartParam);if(sp){track('deep_link_open',sp.game.id,sp.challenge);openGame(sp.game.id,sp.challenge);if(sp.challenge!=null){const higher=sp.game.cfg?.higherIsBetter!==false,txt=challengeIntroText(sp.challenge,higher,sp.game.unit||''),n=el('div','challenge',esc(txt));$('#overlay').appendChild(n);setTimeout(()=>n.remove(),4000)}}if(!CFG.bot)console.warn('[hub] HUB_CONFIG.bot не задан — deep link в шаринге работать не будет')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
